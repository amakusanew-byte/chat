{
  "name": "worker",
  "private": true,
  "scripts": {
    "patch-unrar": "node scripts/patch-unrar.js",
    "dev": "node scripts/patch-unrar.js && wrangler dev",
    "deploy": "node scripts/patch-unrar.js && wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "hono": "^4.6.8",
    "node-unrar-js": "^2.0.2"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240705.0",
    "typescript": "^5.5.4",
    "wrangler": "^4.0.0"
  }
}
